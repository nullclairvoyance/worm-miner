# WORM Multi-Wallet Farming Script - Windows Requirements
# Python 3.9+ required
#
# Install with: pip install --only-binary=:all: -r requirements-windows.txt
# Or run setup-windows.bat for automatic setup

# Blockchain interaction (web3 pulls compatible eth-account)
web3==6.15.0

# HTTP client for remote prover
requests==2.31.0

# Environment configuration
python-dotenv==1.0.1

# CLI colors and formatting
rich==13.7.0
