# WORM Multi-Wallet Farming Script - Windows Requirements
# Python 3.9+ required
# SECURITY: Versions pinned to prevent supply-chain attacks
#
# NOTE: This file uses --only-binary for packages that may fail 
# to build on Windows without Visual C++ Build Tools installed

# Core dependencies (upgrade pip/setuptools first)
# Run: python -m pip install --upgrade pip setuptools wheel

# Blockchain interaction
# Force binary wheel installation to avoid C++ compilation
web3==6.15.0 --only-binary=:all:
eth-account==0.11.0 --only-binary=:all:

# HTTP client for remote prover
requests==2.31.0

# Environment configuration
python-dotenv==1.0.1

# CLI colors and formatting
rich==13.7.0
